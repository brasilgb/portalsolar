import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Locations capturados - Portal Grupo Solar',
};

export default function LocationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
